# CLAUDE.md

Este proyecto usa **`AGENTS.md`** como fuente única de instrucciones para agentes de
código. Léelo primero y seguí su orden de lectura y sus reglas (TDD, límites de módulos,
un slice por agente, Definition of Done).

→ Ver `AGENTS.md` en la raíz del repo.

Arranque: empezá por `openspec/changes/foundation/` y seguí su `tasks.md` test-first.
